# Strelka fixtures

`image.jpg.json` is a reconstructed, minimal fixture based on observed Strelka
output. It is not a complete raw Strelka export. Deliberately bulky fields are
included only to verify that the FileGuard normalizer does not copy them.
